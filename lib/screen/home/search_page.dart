import 'package:flutter/material.dart';

/// Page with list of available food.
class SearchPage extends StatelessWidget {
  /// Page with list of available food.
  const SearchPage();

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Search'));
  }
}
